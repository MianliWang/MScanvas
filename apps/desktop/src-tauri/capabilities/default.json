{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "The main window is granted no Tauri core APIs. The mzML preview reaches the backend only through this application's own commands, and the file picker is owned by Rust rather than by a dialog plugin.",
  "windows": [
    "main"
  ],
  "permissions": []
}
