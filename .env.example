# MSCanvas intentionally has no cloud or telemetry configuration by default.
# ProteoWizard is discovered automatically. When it is installed elsewhere, a
# folder — never an executable — can be chosen in the application, and only for
# the current session. That choice is never stored, so no backend path belongs in
# this file or in any other persisted configuration.
